<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link rel="stylesheet" type="text/css" href="css/styles.css">
    <link rel="stylesheet" href="estilos.css">
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet">
</head>

<body>
    <a href="/"><img class="logoFooter" src="/images/gm-white.png" alt="logo"></a>
    <ul class="errors"></ul>
    <form action="/register" method="POST" id="form" class="cont-form" enctype="multipart/form-data">
        <div class="form">
            <a href="/"><img class="logoHome" src="/images/gm-black.png" alt="logo"></a>
            <h1>Registro</h1>
            <div class="group">
                <input type="text" name="first_name" id="first_name" minlength="3" maxlength="15"><span class="bar"></span>
                <label class="labelLogin" for="first_name">Nombre</label>
                <p class="error">
                    <%= (locals.errors && errors.first_name ? errors.first_name.msg : null) %>
                </p>
            </div>

            <div class="group">
                <input type="text" name="last_name" id="last_name" minlength="3" maxlength="20"><span class="bar"></span>
                <label class="labelLogin" for="last_name">Apellido</label>
                <p class="error">
                    <%= (locals.errors && errors.last_name ? errors.last_name.msg : null) %>
                </p>
            </div>

            <div class="group">
                <input type="text" name="email" id="email"><span class="bar"></span>
                <label class="labelLogin" for="email">Email</label>
                <p class="error">
                    <%= (locals.errors && errors.email ? errors.email.msg : null) %>
                </p>
            </div>
            <div class="group">
                <input type="password" name="password" id="password"><span class="bar"></span>
                <label class="labelLogin" for="password">Password</label>
                <p class="error">
                    <%= (locals.errors && errors.password ? errors.password.msg : null) %>
                </p>
            </div>

            <div class="group">
                <input type="date" id="date" name="date" value="">
                <label class="labelLogin" for="password">Fecha de nacimiento</label>
                <p class="error">
                    <%= (locals.errors && errors.date ? errors.date.msg : null) %>
                </p>
            </div>
            <div class="group">
                <label>Selecciona tu rol</label>
                <select class="select1" name="role_id" id="role_id">
                    <option value="" disabled selected>Selecciona una opcion</option>
                    <option value="1">Administrador</option>
                    <option value="2">Usuario</option>
              </select>
                <p class="error">
                    <%= (locals.errors && errors.role_id ? errors.role_id.msg : null) %>
                </p>
            </div>

            <div class="igroup">
                <input type="file" name="image" id="image"><span class="ibar"></span>
                <label for="image">Imagen</label>
            </div>

            <button type="submit">Suscribirte</button>
        </div>
    </form>
    <script src="/js/registerAddValidation.js"></script>
</body>

</html>