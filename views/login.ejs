<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" type="text/css" href="css/styles.css">
    <link rel="stylesheet" href="estilos.css">
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet">
</head>

<body>
    <a href="/"><img class="logoFooter" src="/images/gm-white.png" alt="logo"></a>
    <ul class="errors"></ul>
    <form action="/login" method="POST" id="form" class="cont-form">
        <div class="form">
            <a href="/"><img class="logoHome" src="/images/gm-black.png" alt="logo"></a>
            <h1>Login</h1>
            <div class="group">
                <% if(typeof msg != 'undefined') {%>
                    <p class="error">
                        <%=msg %>
                    </p>
                    <%  } %>
                        <div class="group">
                            <input type="email" name="email" id="email" value="<%= (typeof email != 'undefined' ? email :null) %>"><span class="bar"></span>
                            <label class="labelLogin" for="email">Email</label>
                            <p class="error">
                                <%= (locals.errors && errors.email ? errors.email.msg : null) %>
                            </p>
                        </div>
                        <div class="group">
                            <input type="password" name="password" id="password" maxlength="10"><span class="bar"></span>
                            <label class="labelLogin" for="password">Password</label>
                            <p class="error">
                                <%= (locals.errors && errors.password ? errors.password.msg : null) %>
                            </p>
                        </div>

                        <div class="cbox1">
                            <label for="remember_password"><input type="checkbox" class="remember_password" id="remember_password" name="remember_password"> Recordar password</label><br>
                        </div>

                        <button type="submit">Ingresar </button>
            </div>

            <div class="group">
                <a href="">Recupera tu contraseña</a>
            </div>
    </form>
    <script src="/js/loginAddValidation.js"></script>
</body>

</html>